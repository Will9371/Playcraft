// Requires Json.Net for Unity

/*
namespace Playcraft.Saving
{
    public static class FlagExtensions 
    {
        static Profile activeProfile => ProfileManager.instance.activeProfile;
    
        public static void SetFlagValue(this string name, bool value)
        {
            activeProfile.savedProperties.SetBool(name, value);
        }

        public static void SetFlagValue(this string name, bool value, Profile profile)
        {
            profile.savedProperties.SetBool(name, value);
        }

        public static bool GetFlagValue(this string name)
        {
            return activeProfile.savedProperties.GetBool(name);
        }

        public static bool GetFlagValue(this string name, Profile profile)
        {
            return profile.savedProperties.GetBool(name);
        }
    }
}
*/
